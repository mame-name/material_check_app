import pandas as pd

def process_data(df):
    """
    読み込んだデータに対して、特定の計算や加工を行う関数。
    例：数値のクリーニングや、新しい列の追加など。
    """
    # 数値列がある場合、欠損値を0で埋める例
    df = df.fillna(0)
    
    # ここに「仕込量」のような計算ロジックを追加できます
    # 例: df['合計'] = df['単価'] * df['数量']
    
    return df
